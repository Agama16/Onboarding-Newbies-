apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pizza-api.fullname" . }}-test-connection"
  labels:
    {{- include "pizza-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pizza-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
