apiVersion: v1
kind: Service
metadata:
  name: {{ include "pizza-api.fullname" . }}
  labels:
    {{- include "pizza-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pizza-api.selectorLabels" . | nindent 4 }}
